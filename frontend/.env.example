# Local dev only. If unset, admin panel silently falls back to the LIVE
# production backend (see adminApi.js) -- always set this for local testing.
# Port 8000 may be taken by another local project -- run manage.py on a free
# port (e.g. 8001) and match it here.
VITE_API_URL=http://localhost:8001/api
